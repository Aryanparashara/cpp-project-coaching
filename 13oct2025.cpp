/*#include<iostream>
int main()
{
  int array[100];
  int i,n,x,flag=0;
  std::cout<<"\n Enter size of an array ";
  std::cin>>n;
  std::cout<<"\n Enter the Elements ";
  for(i=0;i<n;i++)
  {
    std::cin>>array[i];
  }
  std::cout<<"\n Enter the element you want to search ";
  std::cin>>x;
  for(i=0;i<n;i++)
  {
    if(x==array[i])
    flag=1;
  }
  if(flag==1)
  std::cout<<"\n Element found ";
  else 
  std::cout<<"\n Element not found ";
}*/
